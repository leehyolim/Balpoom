<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProductDAO">
	<select id="getProduct" resultType="product">
		select * from product where r_no = #{r_no};		
	</select>
	<select id="getProductGB" resultType="product">
		select p_name, p_price from product where r_no=#{r_no} group by p_name, p_price
	</select>
	<select id="getProductC" resultType="product">
		select p_color from product where r_no=#{r_no} group by p_color;
	</select>
	<select id="getProductS" resultType="product">
		select p_size from product where r_no=#{r_no} group by p_size;
	</select>
</mapper>
