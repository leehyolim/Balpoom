<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProductDAO">
	<select id="getProduct" resultType="product">
		select * from product where r_no = #{r_no};		
	</select>
	<select id="getProductGB" resultType="product">
		select p_name, p_price from product where r_no=#{r_no} group by p_name, p_price
	</select>
	<select id="getProductC" resultType="product">
		select p_color from product where r_no=#{r_no} group by p_color;
	</select>
	<select id="getProductS" resultType="product">
		select p_size from product where r_no=#{r_no} group by p_size;
	</select>
	
	<select id="getOveralls" resultType="overall">
		select distinct p_name,p_price,s.s_busi_name,p_type,p_type_detail from product p,  seller s
		where p.s_no=s.s_no and p_type=#{p_type};
	</select>
	
	<select id="getOverallsDetails" resultType="overall">
		select distinct p_name,p_price,s.s_busi_name,p_type,p_type_detail from product p,  seller s
		where p.s_no=s.s_no and p_type=#{p_type} and p_type_detail=#{p_type_detail};
	</select>
	
	<insert id="insertProduct">
		insert into product set
			p_identifier = #{p_identifier},
			r_no = #{r_no},
			<!-- r_no = 2,-->
			p_name = #{p_name},
			p_type = #{p_type},
			p_price = #{p_price},
			p_s1 = #{p_s1},
			p_s2 = #{p_s2},
			p_s3 = #{p_s3},
			p_s4 = #{p_s4},
			p_s5 = #{p_s5},
			p_color = #{p_color},
			p_size = #{p_size},
			p_common_name = #{p_common_name},
			p_type_detail = #{p_type_detail}
		
	</insert>

</mapper>
